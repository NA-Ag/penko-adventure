import React from 'react';

/**
 * Scanlines overlay component
 * Creates a CRT monitor scanlines effect over the entire application
 */
export const Scanlines: React.FC = () => {
  return <div className="scanlines" aria-hidden="true" />;
};
